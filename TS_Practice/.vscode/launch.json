{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run current file",
            "runtimeExecutable": "npx",
            "runtimeArgs": ["ts-node", "${file}"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
